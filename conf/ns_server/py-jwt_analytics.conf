ns_server.jwt_analytics.JwtAnalytics:
    # JWT bearer auth against the Analytics service. services_init gives a
    # dedicated Analytics-only second node, which is the topology that would
    # catch a hidden assumption that the cbas node also runs kv.
    test_jwt_analytics_bearer_auth,nodes_init=2,services_init=kv:n1ql-cbas,default_bucket=False,use_https=False,GROUP=P0
    test_jwt_analytics_rbac_parity,nodes_init=2,services_init=kv:n1ql-cbas,default_bucket=False,use_https=False,GROUP=P0
    test_jwt_analytics_role_mapping_equivalence,nodes_init=2,services_init=kv:n1ql-cbas,default_bucket=False,use_https=False,GROUP=P0
    test_jwt_analytics_link_parity,nodes_init=2,services_init=kv:n1ql-cbas,default_bucket=False,use_https=False,GROUP=P0
    test_jwt_analytics_role_enforcement,nodes_init=2,services_init=kv:n1ql-cbas,default_bucket=False,use_https=False,GROUP=P0
    test_jwt_analytics_jit_provisioning,nodes_init=2,services_init=kv:n1ql-cbas,default_bucket=False,use_https=False,GROUP=P0
