storage.fusion.fusion_sanity.FusionSanity:

    # Smoke variant: same flow as the real run, week compressed to 5 min so
    # CI can validate the rebalance-cycle/steady-load handoff quickly
    #test_fusion_longevity,num_items=5000000,nodes_init=2,sdk_timeout=60,ops_rate=30000,process_concurrency=8,bucket_ram_quota=256,replicas=1,init_loading=False,standard_buckets=3,magma_buckets=3,doc_size=1024,num_docs_to_validate=1000000,read_ops_rate=5000,read_process_concurrency=8,fusion_log_store_uri=local:///mnt/nfs/share/buckets,monitor_log_store=False,fusion_test=True,num_nodes_to_rebalance_in=1,load_docs_using=sirius_java_sdk,num_weeks=4,week_duration_seconds=300,workload_batch_size=10000,GROUP=longevity_smoke

    # Real run: 4 weeks, one rebalance cycle (nodes_init -> full pool ->
    # nodes_init) per week, steady create/update/read/delete load in between
    test_fusion_longevity,num_items=20000000,nodes_init=2,sdk_timeout=60,ops_rate=30000,process_concurrency=8,bucket_ram_quota=512,replicas=1,init_loading=False,standard_buckets=3,magma_buckets=3,doc_size=1024,num_docs_to_validate=1000000,read_ops_rate=5000,read_process_concurrency=8,fusion_log_store_uri=local:///mnt/nfs/share/buckets,monitor_log_store=False,fusion_test=True,num_nodes_to_rebalance_in=1,load_docs_using=sirius_java_sdk,num_weeks=4,workload_batch_size=100000,GROUP=longevity
