# CRL_Core / Cluster_Ops.Steady_State — mTLS client-cert CRUD against the
# default and a named collection, with clientAuth policy=Require
# (TestCases_CRL.csv rows 112-114). See
# pytests/security/crl_cluster_ops_steady_state.py for what is/isn't
# covered here (rows 115-117, multi-collection transactions, are deferred).

security.crl_cluster_ops_steady_state.CRLClusterOpsSteadyState:
    test_steady_crud_default_collection_revoked_rejected,nodes_init=1,services_init=kv,use_https=True,bucket_size=256,replicas=0,GROUP=P0
    test_steady_crud_named_collection_revoked_rejected,nodes_init=1,services_init=kv,use_https=True,bucket_size=256,replicas=0,GROUP=P0
    test_steady_crud_default_and_named_collection_valid_cert,nodes_init=1,services_init=kv,use_https=True,bucket_size=256,replicas=0,GROUP=P0
