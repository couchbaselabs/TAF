###################################################################################################
# Test GROUPING:
# Sanity coverage for Cloud Snapshot Backup Schedule v4 APIs (cluster-level).
# Endpoint: /v4/organizations/{orgId}/projects/{projId}/clusters/{clusterId}/cloudsnapshotbackupschedule
# Methods covered: GET, PUT (upsert), DELETE.
###################################################################################################

Capella.RestAPIv4.CloudSnapshotBackupSchedule.get_cloud_snapshot_backup_schedule.GetCloudSnapshotBackupSchedule:
    test_api_path,GROUP=P0;functional
    test_authorization,GROUP=P0;security

Capella.RestAPIv4.CloudSnapshotBackupSchedule.update_cloud_snapshot_backup_schedule.UpdateCloudSnapshotBackupSchedule:
    test_api_path,GROUP=P1;functional
    test_authorization,GROUP=P1;security
    test_payload,GROUP=P1;functional

Capella.RestAPIv4.CloudSnapshotBackupSchedule.delete_cloud_snapshot_backup_schedule.DeleteCloudSnapshotBackupSchedule:
    test_api_path,GROUP=P2;functional
    test_authorization,GROUP=P2;security
