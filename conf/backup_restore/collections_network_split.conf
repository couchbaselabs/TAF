# Collections network split with continuous backup verification
# Enables continuous backup and verifies traditional backup + PITR restore after network split recovery
#
# Parameters:
#   continuous_backup_enabled: Enable/disable continuous backup (default: true)
#   continuous_backup_interval: Backup interval in minutes (default: 5)
bucket_collections.collections_network_split.CollectionsNetworkSplit:
    test_collections_crud_with_network_split,nodes_init=4,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=5,cont_bkp_test=NFS,GROUP=P0
    test_quorum_loss_with_network_split,nodes_init=4,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=5,cont_bkp_test=NFS,GROUP=P0
