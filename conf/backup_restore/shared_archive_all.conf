# Shared Backup Archive – Concurrent Access Test Suite
#
# Covers all shared-archive scenarios for cbbackupmgr concurrent access.
#
# The 'provider' parameter does BOTH things with a single flag:
#   1. Tells ProvisionedBaseTestCase which cloud to deploy Capella clusters on.
#   2. Tells SharedArchiveBaseTest which archive scheme to use (s3/gs/az).
#
# All test defaults (cbbackupmgr_path, repo_name, obj_region, sample_bucket,
# backup_threads) are defined in shared_archive_base.py setUp().
# Override on the command line only when needed.
#
# Run the full suite (AWS, default):
#   python testrunner.py \
#     -i node.ini \
#     -c conf/backup_restore/shared_archive_all.conf \
#     -p rerun=False,get-cbcollect-info=False,skip_cluster_reset=True,GROUP=P0
#
# Run on GCP (deploys GCP Capella clusters, uses gs:// archive):
#   Set ADC first: gcloud auth application-default login
#   python testrunner.py \
#     -i node.ini \
#     -c conf/backup_restore/shared_archive_all.conf \
#     -p rerun=False,get-cbcollect-info=False,skip_cluster_reset=True,GROUP=P0:GCP
#
# Run on Azure (deploys Azure Capella clusters, uses az:// archive):
#   python testrunner.py \
#     -i node.ini \
#     -c conf/backup_restore/shared_archive_all.conf \
#     -p rerun=False,get-cbcollect-info=False,skip_cluster_reset=True,GROUP=P0:Azure

# =============================================================================
# AWS  (provider=aws, default)
# Cluster provisioning: AWS   Archive: s3://cbc-storage-<id>/backups
# =============================================================================

backup_restore.shared_archive_happy_path.SharedArchiveHappyPathTest:
    test_happy_path_concurrent_backup_restore,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

backup_restore.shared_archive_rebalance.SharedArchiveRebalanceTest:
    test_rebalance_concurrent_backup_restore,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

backup_restore.shared_archive_rebalance_dest.SharedArchiveRebalanceDestTest:
    test_rebalance_dest_concurrent_backup_restore,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

backup_restore.shared_archive_failover_src.SharedArchiveFailoverSrcTest:
    test_failover_src_node_during_backup_restore,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

backup_restore.shared_archive_failover_dest.SharedArchiveFailoverDestTest:
    test_failover_dest_node_during_backup_restore,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

backup_restore.shared_archive_resume.SharedArchiveResumeTest:
    test_resume_flag_after_interrupted_restore,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

backup_restore.shared_archive_backup_during_restore.SharedArchiveBackupDuringRestoreTest:
    test_backup_starts_after_restore_begins_exclusive_timerange,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0
    test_restore_inclusive_of_active_backup_timerange,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

backup_restore.shared_archive_all_services.SharedArchiveAllServicesTest:
    test_all_services_obj_read_only_and_resume,runtype=dedicated,num_clusters=2,provider=aws,GROUP=P0

# =============================================================================
# Azure  (provider=azure)
# Cluster provisioning: Azure (hostedAzure)   Archive: az://cbc-storage-<id>/backups
# Region: azure_region=eastus2 (default).
# Credentials: cbbackupmgr --capella flag fetches credentials automatically.
# =============================================================================

backup_restore.shared_archive_happy_path.SharedArchiveHappyPathTest:
    test_happy_path_concurrent_backup_restore,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

backup_restore.shared_archive_rebalance.SharedArchiveRebalanceTest:
    test_rebalance_concurrent_backup_restore,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

backup_restore.shared_archive_rebalance_dest.SharedArchiveRebalanceDestTest:
    test_rebalance_dest_concurrent_backup_restore,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

backup_restore.shared_archive_failover_src.SharedArchiveFailoverSrcTest:
    test_failover_src_node_during_backup_restore,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

backup_restore.shared_archive_failover_dest.SharedArchiveFailoverDestTest:
    test_failover_dest_node_during_backup_restore,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

backup_restore.shared_archive_resume.SharedArchiveResumeTest:
    test_resume_flag_after_interrupted_restore,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

backup_restore.shared_archive_backup_during_restore.SharedArchiveBackupDuringRestoreTest:
    test_backup_starts_after_restore_begins_exclusive_timerange,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure
    test_restore_inclusive_of_active_backup_timerange,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

backup_restore.shared_archive_all_services.SharedArchiveAllServicesTest:
    test_all_services_obj_read_only_and_resume,runtype=dedicated,num_clusters=2,provider=azure,azure_region=eastus2,GROUP=P0:Azure

# GCP coverage to be added in a follow-up PR once --capella GCS credential
# support is confirmed for GCP Capella clusters.