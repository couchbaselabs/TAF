# Continuous backup retention test configuration
# This test validates the Continuous Backup Retention features including selection,
# deletion, consolidation, removed node cleanups, crash scenarios, and metrics.
#
# Parameters:
#   bucket_spec: Bucket spec template (default: single_bucket.continuous_backup_tests)
#   continuous_backup_location: Location for continuous backup files (default: /tmp/cont_bkp)
#   continuous_backup_interval: Backup interval in minutes (default: 5)
#   continuous_backup_enabled: Enable/disable continuous backup (default: true)
backup_restore.continuous_backup.continuous_backup_retention_test.ContinuousBackupRetentionTest:
    # 3.1 Configuration & Scheduling Tests
    test_default_state,cont_bkp_test=NFS,nodes_init=3,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=2,retention_test=True,skip_cluster_reset=False,GROUP=P0
    test_valid_configuration_ranges,cont_bkp_test=NFS,nodes_init=3,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=2,retention_test=True,skip_cluster_reset=False,GROUP=P0
    test_invalid_configuration,cont_bkp_test=NFS,nodes_init=3,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=2,retention_test=True,skip_cluster_reset=False,GROUP=P0
    test_execution_scheduling,cont_bkp_test=NFS,nodes_init=3,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=2,retention_test=True,skip_cluster_reset=False,GROUP=P0

    # 3.6 Restore Validation
    test_retention_pitr_restore,cont_bkp_test=NFS,nodes_init=3,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=2,retention_test=True,skip_cluster_reset=False,GROUP=P0