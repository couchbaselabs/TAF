# Flush tests with continuous backup verification
# Enables continuous backup and verifies traditional backup + PITR restore after flush operations
#
# Parameters:
#   continuous_backup_enabled: Enable/disable continuous backup (default: true)
#   continuous_backup_interval: Backup interval in minutes (default: 5)
bucket_collections.flush_test.FlushTests:
    test_flush_bucket_without_mutations,nodes_init=4,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=5,cont_bkp_test=NFS,GROUP=P0
    test_flush_bucket_during_mutations,nodes_init=4,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=5,cont_bkp_test=NFS,GROUP=P0
    test_data_post_flush,nodes_init=4,bucket_spec=single_bucket.continuous_backup_tests,data_spec_name=initial_load,continuous_backup_enabled=True,continuous_backup_interval=5,cont_bkp_test=NFS,GROUP=P0
